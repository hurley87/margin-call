import { defineSchema } from "convex/server";

/**
 * Empty schema — Crash keeper and desk-phone tables have been retired.
 * Future product tables land here through separately scoped work.
 */
export default defineSchema({});
